# Minimal runtime dependencies for deployment on Render (production)
# Keep this file lean: no Jupyter, no dev tools. If you deploy via Docker, prefer pinning in the image.

fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.4
joblib==1.3.2
numpy==1.26.4
pandas==2.0.3
scikit-learn==1.3.2
alpha_vantage==3.0.0
requests==2.32.5
python-dotenv==1.2.1
prometheus-fastapi-instrumentator==6.0.0
evidently>=0.7.17

# If your deployment must load the Keras/TensorFlow model, include TF and h5py.
# Note: tensorflow is large and can cause long build times; consider using a Docker image
# that already includes TF, or host the model in a separate service.
tensorflow==2.10.1
h5py==3.8.0
