# Exemplo de variáveis de ambiente para configuração de LLM e API
# Copie para `.env` (ou defina no ambiente/container) e preencha com suas credenciais.

# Provedor de LLM (ex: 'anthropic', 'openai', 'azure', 'local')
LLM_PROVIDER=

# Nome do modelo a usar — aqui você pode colocar 'claude-sonnet-3.5' quando sua conta já tiver acesso
LLM_MODEL=claude-sonnet-3.5

# Chave de API para o provedor escolhido
LLM_API_KEY=

# Endpoint adicional (opcional) — útil para provedores corporativos ou proxies
LLM_API_BASE_URL=

# Tempo limite para requisições ao LLM (segundos)
LLM_TIMEOUT=30
